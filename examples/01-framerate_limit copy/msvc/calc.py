import pickle
import array
import os
try:
    import numpy as np
    with open('D:\\programming\\auto_remaster\\reshade\examples\\01-framerate_limit copy\\msvc\\temp.test', 'w',) as f:
        f.write('completed')
except Exception as e:
    with open('D:\\programming\\auto_remaster\\reshade\examples\\01-framerate_limit copy\\msvc\\temp.test', 'w',) as f:
        f.write(str(e))
        # os.
import ctypes
def add(x, y, array):
# def add(x, y):
    # print("array 123", array)
    # array[0][0] = 666
    # CharArray = ctypes.c_uint8 * 1920*1080*4
    # buffer_array = CharArray.from_address(array)
    # print(buffer_array[1000000])
    # buffer_array.raw
    # buffer_array = CharArray.from_buffer(array)
    # buffer_array.from_buffer
    # chr(buffer_array[0])
    # buffer_content = "".join([str(buffer_array[i]) for i in range(10)])
    # with open('D:\\programming\\auto_remaster\\reshade\examples\\01-framerate_limit copy\\msvc\\temp.pickle', 'wb',) as f:
    #     # Use pickle.dump() to serialize the data and write it to the file
    #     pickle.dump(buffer_array, f)
    # with open('D:\\programming\\auto_remaster\\reshade\examples\\01-framerate_limit copy\\msvc\\temp.test', 'w',) as f:
    #     f.write(buffer_content)
    # return array
    # try:
    #     array = np.frombuffer(array, dtype=np.int32)
    # except Exception as e:
    #     with open('D:\\programming\\auto_remaster\\reshade\examples\\01-framerate_limit copy\\msvc\\temp2.test', 'w',) as f:
    #         f.write(str(e))
    return 1+1
    # return np.array([123]).tolist()


# test new text